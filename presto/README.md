# coordinator

```

// 1.启动Server命令
> ./bin/launcher start
> ./bin/launcher stop

// 2.启动Client命令
> ./bin/presto-cli --server node101:5050 --catalog hive --schema default

// or
> ./bin/presto-cli --server node101:5050 --catalog mysql --schema system

// web ui


```

# worker

```
// 1.启动Server命令
> ./bin/launcher start
> ./bin/launcher stop

```
